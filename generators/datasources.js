// TODO: generate datasources from source

// load the datasource
var sourceDir = "./test/testapp1/.makomi/"
var datasourceName = "maindb"
mkSrc.loadDatasource(sourceDir,datasourceName,function(datasource) {
})